<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NG.ServiceWorker.UI.ListUI"
             xmlns:ui="clr-namespace:NG.ServiceWorker.UI;assembly=NG.ServiceWorker.UI.AppControls"
             x:Class="NG.ServiceWorker.UI.ListUI.ListItemCell">
    <Grid Style="{StaticResource BackgroundGrid}">
        <Grid BackgroundColor="{AppThemeBinding Light={StaticResource ListSelectionLight}, Dark={StaticResource ListSelectionDark}}" IsVisible="{Binding IsSelectedVisible}"/>
        <local:ListItemLayoutView>
            <Label Style="{StaticResource ListItemMainLabel}" Text="{Binding MainText}"/>
            <Label Style="{StaticResource ListItemSubtitleLabel}" Text="{Binding ExtraText}" IsVisible="{Binding IsSubtitleVisible}"/>
            <Label Style="{StaticResource ListItemAttributeLabel}" Text="{Binding ExtraText}" IsVisible="{Binding IsAttributeValueVisible}"/>
            <ui:SvgImage SvgName="ui_list_disclosure"
                         RenderColour="{AppThemeBinding Light={StaticResource ForegroundDullLight}, Dark={StaticResource ForegroundDullDark}}"
                         WidthRequest="9" HeightRequest="15"/>
        </local:ListItemLayoutView>
    </Grid>
</ViewCell>
