<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:jobs="clr-namespace:NG.ServiceWorker.Jobs"
             xmlns:map="clr-namespace:NG.ServiceWorker.MapUI"
             xmlns:system="clr-namespace:NG.ServiceWorker.SystemUI"
             x:Class="NG.ServiceWorker.MainPage">
    <jobs:JobListPage Title="Jobs" IconImageSource="{Binding JobsTabIconImage}"/>
    <map:MapPage Title="Map" IconImageSource="{Binding MapTabIconImage}"/>
    <NavigationPage Title="System" IconImageSource="{Binding SystemTabIconImage}">
        <x:Arguments>
            <system:SystemPage Title="System"/>
        </x:Arguments>
    </NavigationPage>
</TabbedPage>
